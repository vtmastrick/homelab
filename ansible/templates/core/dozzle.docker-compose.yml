services:
  dozzle:
    container_name: dozzle
    image: amir20/dozzle:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - 8908:8080
    networks:
      - dozzle
      - nginx-bridge
      
networks:
  dozzle:
  nginx-bridge:
    external: true